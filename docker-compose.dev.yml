
services:
  db:
    image: mcr.microsoft.com/mssql/server:2019-latest
    container_name: prueba-db-sqlserver
    ports:
      - '${DB_PORT}:1433'
    environment:
      ACCEPT_EULA: 'Y'
      MSSQL_SA_PASSWORD: ${DB_PASSWORD}
    volumes:
      - sqlserver-data:/var/opt/mssql
    mem_limit: 4g
    mem_reservation: 2g
    networks:
      - default
  
  task-api:
    depends_on:
      - db
    container_name: document-generator
    build:
      context: .
      dockerfile: Dockerfile.dev
    restart: always
    volumes:
      - ./src:/usr/src/app/src
    env_file:
      ".env"
    ports:
      - "${PORT}:3000" 
    command: pnpm start:dev
    networks:
      - default



volumes:
  sqlserver-data:
    driver: local

